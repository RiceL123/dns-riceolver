{
  "name": "dns-resolver",
  "version": "1.0.0",
  "description": "## Basic implementation `client.py` works with `resolver.py` correctly",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node server.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^4.21.2"
  }
}
